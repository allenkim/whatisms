# Plaid API credentials
# Get these from https://dashboard.plaid.com/developers/keys

# Your Plaid client ID
PLAID_CLIENT_ID=your_client_id_here

# Your Plaid secret (use sandbox secret for testing)
PLAID_SECRET=your_secret_here

# Environment: sandbox, development, or production
# - sandbox: Use for testing with fake data
# - development: Use for testing with real banks (100 free connections)
# - production: Use for production (paid)
PLAID_ENV=sandbox

# Database URL (LibSQL file path)
# In Docker, set to file:/app/data/finance.db for persistent volume storage.
# For local dev, leave unset to use the default (file:prisma/dev.db).
# DATABASE_URL=file:/app/data/finance.db

# SnapTrade API credentials (for brokerages not supported by Plaid, e.g. Fidelity)
# Get these from https://docs.snaptrade.com/ or your SnapTrade dashboard
SNAPTRADE_CLIENT_ID=your_snaptrade_client_id_here
SNAPTRADE_CONSUMER_KEY=your_snaptrade_consumer_key_here
